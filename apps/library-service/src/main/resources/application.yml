server:
  port: ${SERVER_PORT:8080}
spring:
  application:
    name: library-service
  jackson:
    default-property-inclusion: non_null
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
app:
  version: ${APP_VERSION:library-local}
  fault-mode: ${FAULT_MODE:HEALTHY}
